# yaml-language-server: $schema=../../schemas/yaml/services_schema.yaml
# This integration does not expose custom services.
# Its purpose is to call existing Home Assistant services and expose
# their response data through sensor entities.
